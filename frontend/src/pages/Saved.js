import React from 'react';
import { Typography, Box } from '@mui/material';

const Saved = () => {
  return (
    <Box>
      <Typography variant="h4">Saved Companies</Typography>
      <Typography>No saved companies yet</Typography>
    </Box>
  );
};

export default Saved;